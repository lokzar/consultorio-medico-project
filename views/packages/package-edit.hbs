<div class="p-5 text-center">
    <h2>Edita el paquete: {{package.name}}</h2>

    <form class="form-control" action="/package/{{package._id}}/edit" method="post">
        <label class="labelClass"> Nombre:
            <br>
            <input type="text" name="name" value="{{package.name}}" />
        </label>
        <br>
        <label class="labelClass"> Incluye:
            <br>
            <textarea name="description" cols="30" rows="4">{{package.description}}</textarea>
        </label>
        <br>
        <label class="labelClass"> Precio:
            <br>
            <input type="text" name="precio" value="{{package.price}}" />
        </label>
        <br>
        <button class="btn btn-primary rounded-pill mt-3"> Guardar cambios </button>
    </form>

    <a class="btn btn-primary rounded-pill mt-3" href="/package/list">Regresar a la lista</a>

</div>